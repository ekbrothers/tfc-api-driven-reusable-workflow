name: Terraform Plan on Push to Main

on:
  push:
    branches:
      - main

env:
  TF_CLOUD_ORGANIZATION: "testing-workspaces"
  TF_API_TOKEN: "${{ secrets.TF_API_TOKEN }}"
  TF_WORKSPACE: "tfc-api-driven-reusable-workflow"
  WORKING_DIRECTORY: "./"

jobs:
  terraform-plan:
    name: Dev Provisioning - Demo
    uses: ekbrothers/tfc-api-driven-reusable-workflow/.github/workflows/tfc-api-driven-reusable-workflow.yaml@29216be1c19c05c43e950c586271d22876f6eea8
    with:
      env: dev
    # secrets:
    #   TF_API_TOKEN: ${{ secrets.TF_API_TOKEN }}
